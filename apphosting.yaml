# apphosting.yaml
# https://firebase.google.com/docs/app-hosting/configure :contentReference[oaicite:4]{index=4}

runConfig:
  maxInstances: 1

cors:
  - origin: "*"
    method: ["GET", "HEAD"]

# Firebase Admin uses the App Hosting backend's managed service identity.
# Configure model-provider secrets such as GEMINI_API_KEY in the Firebase console.
